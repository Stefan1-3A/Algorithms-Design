#ifndef DATAGENERATOR_H
#define DATAGENERATOR_H

void generateData(const char *filename, int count);                         // This function serves to generate data into a specified file with a specified count of random book sizes

#endif // DATAGENERATOR
